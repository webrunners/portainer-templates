[
  {
  "type": "stack",
  "title": "Runner",
  "description": "Custom GitLab Runner",
  "repository": {
      "url": "https://github.com/webrunners/gitlab-runner-service",
      "stackfile": "runner.yml"
    },
  "note": "First create a docker secret, named like 'Name'. You must define TOKEN env variable in there. Name should end with '_runner'.  Additionally, you could create a docker config named 'runner', containing defaults.",
  "env": [
      {
      "name": "EXECUTOR",
      "label": "EXECUTOR ([shell|docker] Default: shell)"
      },
      {
      "name": "TAG_LIST",
      "label": "TAG_LIST (Tag this runner. Comma separated)"
      },
      {
      "name": "CI_SERVER_URL",
      "label": "CI_SERVER_URL (GitLab ci url. Usually GitLab-URL/ci.)"
      }
    ]
  },
  {
  "type": "stack",
  "title": "WhoamEye",
  "description": "Service Example",
  "repository": {
      "url": "https://github.com/webrunners/gitlab-runner-service",
      "stackfile": "teststack.yml"
    },
  "env": [
      {
      "name": "DOMAIN",
      "label": "DOMAIN"
      },
      {
      "name":  "SERVICE_PORT",
      "label": "SERVICE_PORT"
      }
    ]
  }
]