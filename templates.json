[
  {
  "type": "stack",
  "title": "Runner",
  "description": "Custom GitLab Runner",
  "repository": {
      "url": "https://github.com/webrunners/gitlab-runner-service",
      "stackfile": "runner.yml"
    },
  "note": "First create a docker secret, named like SERVICE. You must define TOKEN env variable in there. Name of stack should end with '_runner'",
  "env": [
      {
      "name": "EXECUTOR",
      "label": "EXECUTOR ([shell|docker] Default: shell)"
      },
      {
      "name": "TAG_LIST",
      "label": "TAG_LIST (Tag this runner. Comma separated)"
      }
    ]
  },
  {
  "type": "stack",
  "title": "WhoamEye",
  "description": "Service Example",
  "repository": {
      "url": "https://github.com/webrunners/gitlab-runner-service",
      "stackfile": "teststack.yml"
    },
  "env": [
      {
      "name": "DOMAIN",
      "label": "DOMAIN"
      },
      {
      "name":  "SERVICE_PORT",
      "label": "SERVICE_PORT"
      }
    ]
  }
]